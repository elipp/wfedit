#version 400

in float dydx;
out vec4 frag_color;

uniform float TIME;

float S(float t) {
	return 0.5*sin(t) + 0.5;
}


void main() {
  frag_color = vec4(
  S(pow(S(TIME*dydx), dydx)),
  S(TIME*S(TIME*dydx)),
  S(dydx*S(TIME*dydx)),
  1.0);
}
